name: Hugo Blog

on:
  push:
    branches: [main]
    paths:
      - 'posts/**/*.md'
      - 'assets/figures/**'
      - 'hugo.toml'
      - 'layouts/**'
  workflow_dispatch:

concurrency:
  group: hugo-pages
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: '0.147.0'
          extended: true

      - name: Create content symlinks
        run: |
          mkdir -p content
          ln -sfn ../posts content/posts
          ln -sfn ../assets content/assets

      - name: Download PaperMod theme
        run: |
          mkdir -p themes
          curl -sL https://github.com/adityatelange/hugo-PaperMod/archive/refs/tags/v8.0.tar.gz | tar xz -C themes
          mv themes/hugo-PaperMod-* themes/PaperMod

      - name: Build with Hugo
        run: hugo --minify --gc

      - name: Install Pagefind
        run: |
          mkdir -p .pagefind-bin
          curl -sL https://github.com/CloudCannon/pagefind/releases/download/v1.3.0/pagefind-v1.3.0-x86_64-unknown-linux-musl.tar.gz | tar xz -C .pagefind-bin
          chmod +x .pagefind-bin/pagefind

      - name: Run Pagefind
        run: .pagefind-bin/pagefind --site public

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          publish_branch: gh-pages
          force_orphan: false
          user_name: 'github-actions[bot]'
          user_email: 'github-actions[bot]@users.noreply.github.com'
